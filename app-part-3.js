window.__LM_CODE=(window.__LM_CODE||"")+"n\">Choose an optional task</button></section>`}\n      <section class=\"quick-grid\" aria-label=\"Quick actions\">\n        <button class=\"quick-action\" data-action=\"something-changed\" type=\"button\"><span class=\"quick-icon\" aria-hidden=\"true\">\u21bb</span><span><strong>Something changed</strong><small>Update the room state</small></span><span aria-hidden=\"true\">\u203a</span></button>\n        <button class=\"quick-action\" data-action=\"choose-task\" type=\"button\"><span class=\"quick-icon\" aria-hidden=\"true\">\u2637</span><span><strong>Choose another task</strong><small>Browse all tasks</small></span><span aria-hidden=\"true\">\u203a</span></button>\n      </section>\n    </section>`;\n  }\n\n  function renderTasks() {\n    const filtered = state.tasks.filter(task => {\n      const matchFilter = currentTaskFilter === 'all' || task.mode === currentTaskFilter || task.type === currentTaskFilter || task.category === currentTaskFilter;\n      const haystack = `${task.title} ${getCategoryMeta(task.category).label} ${task.firstStep}`.toLowerCase();\n      return matchFilter && haystack.includes(taskSearch.toLowerCase());\n    });\n    const taskCards = filtered.map(task => {\n      const meta = getCategoryMeta(task.category);\n      return `<button class=\"list-card task-card category-${escapeHtml(task.category)}\" data-action=\"open-task\" data-task-id=\"${escapeHtml(task.id)}\" type=\"button\">\n        <span class=\"category-label\"><span aria-hidden=\"true\">${meta.icon}</span>${escapeHtml(meta.label)}</span>\n        <strong>${escapeHtml(task.title)}</strong>\n        <p>${escapeHtml(task.firstStep)}</p>\n      </button>`;\n    }).join('');\n\n    viewRoot.innerHTML = `<section class=\"screen\" aria-labelledby=\"tasks-title\">\n      <div class=\"screen-title\"><div><h2 id=\"tasks-title\">Tasks</h2><p>Browse, add and edit.</p></div><button class=\"icon-button\" data-action=\"open-add-menu\" aria-label=\"Add task or category\" type=\"button\">+</button></div>\n      <label class=\"search-label\">Search tasks<input class=\"search-input\" id=\"taskSearch\" type=\"search\" value=\"${escapeHtml(taskSearch)}\" placeholder=\"Dishes, bed, rubbish\u2026\"></label>\n      <div class=\"filter-row\" aria-label=\"Task filters\">\n        ${filterChip('all', 'All')}${filterChip('recovery', 'Recovery')}${filterChip('maintenance', 'Maintenance')}${filterChip('oneoff', 'One-off')}\n      </div>\n      <div class=\"task-list\">${taskCards || `<section class=\"empty-state\"><h3>No matching tasks</h3><p>Clear the search or add a new task.</p></section>`}</div>\n    </section>`;\n    document.getElementById('taskSearch').addEventListener('input', event => { taskSearch = event.target.value; renderTasks(); });\n  }\n\n  function filterChip(value, label) {\n    return `<button class=\"chip ${currentTaskFilter === value ? 'active' : ''}\" data-action=\"filter-tasks\" data-filter=\"${value}\" type=\"button\">${label}</button>`;\n  }\n\n  function renderAdvice() {\n    const records = state.advice.filter(item => `${item.title} ${item.type} ${item.summary}`.toLowerCase().includes(adviceSearch.toLowerCase()));\n    if (activeAdviceId) {\n      const item = state.advice.find(advice => advice.id === activeAdviceId);\n      if (!item) activeAdviceId = null;\n      else {\n        viewRoot.innerHTML = `<section class=\"screen\" aria-labelledby=\"advice-detail-title\">\n          <div class=\"screen-title\"><div><h2 id=\"advice-detail-title\">${escapeHtml(item.title)}</h2><p>${escapeHtml(item.type)}</p></div><button class=\"icon-button\" data-action=\"close-advice\" aria-label=\"Back to advice library\" type=\"button\">\u2190</button></div>\n          <article class=\"panel advice-card\">\n            <p><strong>${escapeHtml(item.summary)}</strong></p>\n            <p>${escapeHtml(item.body)}</p>\n          </article>\n          <button class=\"secondary-button\" data-action=\"close-advice\" type=\"button\">Back to Advice</button>\n        </section>`;\n        return;\n      }\n    }\n    viewRoot.innerHTML = `<section class=\"screen\" aria-labelledby=\"advice-title\">\n      <div class=\"screen-title\"><div><h2 id=\"advice-title\">Advice</h2><p>Books, methods and your rules.</p></div></div>\n      <label class=\"search-label\">Search advice<input id=\"adviceSearch\" class=\"search-input\" type=\"search\" value=\"${escapeHtml(adviceSearch)}\" placeholder=\"One bag, storage, minimum\u2026\"></label>\n      <div class=\"advice-list\">${records.map(item => `<button class=\"list-card advice-card\" data-action=\"open-advice\" data-advice-id=\"${escapeHtml(item.id)}\" type=\"button\"><span class=\"status-pill\">${escapeHtml(item.type)}</span><strong>${escapeHtml(item.title)}</strong><p>${escapeHtml(item.summary)}</p></button>`).join('')}</div>\n    </section>`;\n    document.getElementById('adviceSearch').addEventListener('input', event => { adviceSearch = event.target.value; renderAdvice(); });\n  }\n\n  function renderMore() {\n    viewRoot.innerHTML = `<section class=\"screen\" aria-labelledby=\"more-title\">\n      <div class=\"screen-title\"><div><h2 id=\"more-title\">More</h2><p>History, wardrobe, categories and settings.</p></div></div>\n      <div class=\"more-grid\">\n        <section class=\"more-section\"><h3>Records</h3>\n          ${moreRow('\u25f7', 'History and resume', 'See exact completed and next actions', 'show-history')}\n          ${moreRow('\u2659', 'Wardrobe and clothing', 'Log items gradually', 'show-wardrobe')}\n        </section>\n        <section class=\"more-section\"><h3>Manage</h3>\n          ${moreRow('\u25a3', 'Permanent categories', 'Add or rename reusable categories', 'manage-categories')}\n          ${moreRow('\u2301', 'Index / I\u2019m lost', 'Find any app action by scenario', 'open-index')}\n        </section>\n        <section class=\"more-section\"><h3>Settings and data</h3>\n          ${moreRow('\u25d0', 'Appearance', `Current: ${state.appearance}`, 'appearance-settings')}\n          ${moreRow('\u21e9', 'Export backup', 'Download your local app data', 'export-data')}\n          ${moreRow('\u21e7', 'Import backup', 'Restore a previous JSON backup', 'import-data')}\n          ${moreRow('\u21ba', 'Reset sample data', 'Return to the starter version', 'reset-data')}\n        </section>\n      </div>\n    </section>`;\n  }\n\n  function moreRow(icon, title, detail, action) {\n    return `<button class=\"row-button\" data-action=\"${action}\" type=\"button\"><span class=\"quick-icon\" aria-hidden=\"true\">${icon}</span><span><strong>${escapeHtml(title)}</strong><small>${escapeHtml(detail)}</small></span><span aria-hidden=\"true\">\u203a</span></button>`;\n  }\n\n  function renderActiveTask() {\n    const session = state.activeSession;\n    if (!session) { setView('main'); return; }\n    const task = getTask(session.taskId);\n    const meta = getCategoryMeta(task.category);\n    const stepIndex = Math.min(session.stepIndex, task.steps.length - 1);\n    const step = task.steps[stepIndex];\n    const isLoopQuestion = task.loop && stepIndex === task.steps.length - 1;\n\n    viewRoot.innerHTML = `<section class=\"screen\" aria-labelledby=\"active-title\">\n      <div class=\"screen-title\"><div><h2 id=\"active-title\">Active task</h2><p>${escapeHtml(session.version === 'minimum' ? 'Minimum' : 'Full')} version</p></div";
